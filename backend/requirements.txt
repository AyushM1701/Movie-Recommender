# CineMatch — Python Backend Dependencies
# Install with: pip install -r requirements.txt

# Web framework & server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9       # needed for form data

# Data & ML
pandas>=2.2.3,<3.1
numpy>=2.1,<3
scikit-learn>=1.6,<1.8

# Database ORM
sqlalchemy>=2.0.30

# Data validation (FastAPI uses this internally; pin for stability)
pydantic>=2.9.2

# Auth and API testing
bcrypt==5.0.0
python-jose==3.5.0
httpx==0.28.1

# Optional: PostgreSQL driver (uncomment if switching from SQLite)
psycopg2-binary==2.9.9
